# Fast concatenation of XCMS peak picked files

Fast concatenation of XCMS peak picked files

## Usage

``` r
cFindChromPeaksStep2(inputs, output)
```

## Arguments

- inputs:

  the peak picked files

- output:

  the msnbase element of peak picked files

## Value

an XcmsExperiment ready for peak grouping and alignment
